## 24.0.5-07 — `notifier/NotifierHost.kt` (placeholder)

Create new file `app/src/main/java/com/icodeforyou/catscarscoins/notifier/NotifierHost.kt` (**Project view:** right-click `java/com.icodeforyou.catscarscoins` → New → Package → `notifier`, then New → Kotlin Class/File → File → `NotifierHost`). Full content:

```kotlin
// notifier/NotifierHost.kt
package com.icodeforyou.catscarscoins.notifier

import androidx.compose.runtime.Composable

/**
 * Placeholder host for the custom in-app Notifier (spec §5: arbitrary
 * composable payload, dismissible; first built for real in Phase 2).
 * At standup it renders content untouched — the call site in MainActivity
 * is wired now so Phase 2 replaces only this body, never the callers (OCP).
 */
@Composable
fun NotifierHost(content: @Composable () -> Unit) {
    content()
}
```

**Why:** spec 0.5 puts `NotifierHost(placeholder)` in the `setContent` tree. Landing the placeholder before the MainActivity rewrite that calls it keeps the next step a pure consumer — the standup wiring is permanent, only the host's internals evolve.

### Verify

```zsh
./gradlew assembleDebug
```
