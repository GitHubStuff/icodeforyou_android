## 24.0.5-12 — Commit the 24.0.5 Set

Build is green. Commit the app skeleton. From the repo root (`icodeforyou_android`):

```zsh
git add CatsCarsCoins/app/src/main/java CatsCarsCoins/app/src/main/AndroidManifest.xml CatsCarsCoins/gradle/libs.versions.toml CatsCarsCoins/app/build.gradle.kts
git status
```

Expected staged: `di/AppModules.kt`, `MyApp.kt`, `nav/AppNavKeys.kt`, `notifier/NotifierHost.kt`, `ui/theme/Theme.kt`, `MainActivity.kt`, `AndroidManifest.xml`, `libs.versions.toml`, `app/build.gradle.kts`. Must NOT appear: `build/`, `.gradle/`, `local.properties`.

```zsh
git commit -m "feat(CatsCarsCoins): 24.0.5 app skeleton — Koin startup, AppTheme (dynamic off), splash handoff, Nav3 Splash->Main in NavigationSuiteScaffold + NotifierHost placeholder"
git push
```

**Why:** 24.0.5 is one vertical slice of app shell — DI bootstrap through navigation chrome — and it only runs as a whole, so it lands as one commit. The toml/gradle files ride along for the `material-icons-core` addition that the shell required.
