# 24.0.5-16 — Remove Dead Code in `Theme.kt`

**Project:** CatsCarsCoins (`com.icodeforyou.catscarscoins`)
**Spec section:** 24.0.5 — AppTheme + NavigationSuiteScaffold + NotifierHost + NavDisplay
**File delivered:** `app/src/main/java/com/icodeforyou/catscarscoins/ui/theme/Theme.kt` (complete file)
**Date:** 2026-07-05

---

## Problem

`AppTheme` contained a stray bare `MaterialTheme` expression statement
directly above the real `MaterialTheme(...)` call:

```kotlin
    MaterialTheme          // ← dead code: a no-op object reference
    MaterialTheme(
        colorScheme = ...
```

It compiles (referencing the `MaterialTheme` object is a valid expression)
but does nothing — dead code left over from an earlier edit. Clean Code:
dead code is deleted, not tolerated.

## Changes

- **Removed** the bare `MaterialTheme` statement. One line deleted.
- No other lines touched: palettes, `AppTheme` signature, and the
  deterministic no-dynamic-color decision are unchanged.

## Verification gate

```
./gradlew assembleDebug
```

Builds clean; theme rendering identical (the removed line had no effect).

---

## Complete file: `Theme.kt`

```kotlin
// ui/theme/Theme.kt
// CatsCarsCoins — spec 24.0.5. Complete file.
// Change from previous version: removed the stray bare `MaterialTheme`
// expression statement (dead code — a no-op object reference) that sat
// directly above the real MaterialTheme(...) call.
package com.icodeforyou.catscarscoins.ui.theme

import androidx.compose.foundation.isSystemInDarkTheme
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.darkColorScheme
import androidx.compose.material3.lightColorScheme
import androidx.compose.runtime.Composable

private val DarkColorScheme = darkColorScheme(
    primary = Purple80,
    secondary = PurpleGrey80,
    tertiary = Pink80,
)

private val LightColorScheme = lightColorScheme(
    primary = Purple40,
    secondary = PurpleGrey40,
    tertiary = Pink40,
)

/**
 * App-wide Material3 theme (spec 0.5): explicit light/dark palettes,
 * dynamic color deliberately unsupported — palettes stay deterministic
 * across devices, which a reference blueprint requires.
 */
@Composable
fun AppTheme(
    darkTheme: Boolean = isSystemInDarkTheme(),
    content: @Composable () -> Unit,
) {
    MaterialTheme(
        colorScheme = if (darkTheme) DarkColorScheme else LightColorScheme,
        typography = Typography,
        content = content,
    )
}
```
