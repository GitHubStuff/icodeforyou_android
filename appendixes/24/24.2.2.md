# 24.2.2 — Phase 2: `CoinsRepository` Domain Contract

**Project:** CatsCarsCoins (`com.icodeforyou.catscarscoins`)
**Spec section:** 24.2 — Coins (Phase 2)
**File delivered:** `app/src/main/java/com/icodeforyou/catscarscoins/coins/domain/CoinsRepository.kt` (complete file, new)
**Date:** 2026-07-05

---

## Design

- **Pure domain**: sole import is `Flow`; Room stays behind the data layer.
- **Three members, each a spec clause:**
  - `coins` — newest-first observation, the shape both the Coins list and
    the Database Viewer's COINS segment consume.
  - `record(amountCents, recordedAtEpochMillis)` — append + mandatory
    oldest-first trim to `Coin.ROW_CAP`. The trim is contract, not
    implementation courtesy: every implementer and test builds to it.
  - `clearAll()` — the Reset App hook (spec §16 delegates table clearing
    to each feature repository; no central schema knowledge).
- **Dedup deliberately absent.** Spec Decision Log: consecutive-only dedup
  lives in the polling pipeline via `distinctUntilChanged` on
  `amountCents`. Stating the exclusion on the interface stops a future
  implementer from "helpfully" adding a second dedup layer.
- Primitive parameters on `record` rather than a `Coin` — the id is
  Room-assigned; accepting a `Coin` would invite callers to invent ids.

## Verification gate

```
./gradlew assembleDebug
```

Compiles clean; no implementation exists yet.

---

## Complete file: `CoinsRepository.kt`

```kotlin
// coins/domain/CoinsRepository.kt
// CatsCarsCoins — spec 24.2.2. Complete file.
package com.icodeforyou.catscarscoins.coins.domain

import kotlinx.coroutines.flow.Flow

/**
 * Domain contract for stored BTC-USD price samples (spec: coins table,
 * FIFO cap 500). Pure Kotlin — the data layer implements it over Room;
 * consumers depend only on this interface (DIP).
 *
 * Scope note: **no dedup here.** The spec's Decision Log places the
 * consecutive-only dedup in the polling pipeline
 * (`distinctUntilChanged` on amountCents), so this contract is storage
 * only — a second dedup layer would be a second source of truth for the
 * feature's core rule.
 */
interface CoinsRepository {

    /**
     * All stored samples, newest first ([Coin.recordedAtEpochMillis]
     * descending), emitting on every change.
     */
    val coins: Flow<List<Coin>>

    /**
     * Appends one sample. Implementations MUST trim oldest-first to
     * [Coin.ROW_CAP] rows after the insert (spec: FIFO 500) — the cap is
     * a persistence invariant, not a caller courtesy.
     */
    suspend fun record(amountCents: Long, recordedAtEpochMillis: Long)

    /**
     * Deletes every sample. Invoked by ResetAppUseCase (spec §16: each
     * feature repository clears its own tables) and by nothing else with
     * confirmation — spec-wide rule: no confirm on destructive actions.
     */
    suspend fun clearAll()
}
```
