# 24.2.42 — Phase 2: Koin `notifierModule`

**Project:** CatsCarsCoins (`com.icodeforyou.catscarscoins`)
**Spec section:** 24.2 — Coins (Phase 2); spec Notifier
**File delivered:** `app/src/main/java/com/icodeforyou/catscarscoins/notifier/NotifierModule.kt` (complete file, new)
**Date:** 2026-07-05

---

## Design

- **One `single { Notifier() }`.** Process-wide by necessity, not habit:
  every producer and the one host must share the same slot, or
  replace-on-collision fragments into per-instance policies.
- Own module in the `notifier/` package — shell infrastructure follows the
  same module-per-unit convention as the features; `AppModules` (next
  step) stays the only aggregation point.

## Verification gate

```
./gradlew assembleDebug
```

Compiles clean; not yet in `appModules` — a toast before the next step
would crash `koinInject` at composition. Wire before showing.

---

## Complete file: `NotifierModule.kt`

```kotlin
// notifier/NotifierModule.kt
// CatsCarsCoins — spec 24.2.42. Complete file.
package com.icodeforyou.catscarscoins.notifier

import org.koin.dsl.module

/**
 * Notifier wiring: one process-wide [Notifier] — a single slot shared by
 * every producer (coin toast, read-only toast, whatever comes later) and
 * the one NotifierHost that renders it. Two instances would mean two
 * competing collision policies; the single IS the spec's
 * replace-on-collision at the object level.
 */
val notifierModule = module {

    single {
        Notifier()
    }
}
```
