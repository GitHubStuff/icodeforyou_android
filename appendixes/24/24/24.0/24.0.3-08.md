## 24.0.3-08 — Expected Warnings + Commit

### Warnings that remain after sync — expected, ignore all four

1. "A newer version of org.jetbrains.kotlin.plugin.compose than 2.2.10 is available" — spec pin (Kotlin 2.2.10). Moves by spec revision, never the IDE banner.
2. "A newer version of androidx.compose:compose-bom than 2026.02.01 is available" — spec pin. Same rule.
3. "A newer version of org.jetbrains.kotlinx:kotlinx-serialization-json than 1.9.0 is available" — compiler/runtime pairing pin: 1.9.0 is the Kotlin 2.2 line; the nag doesn't know about Kotlin-line matching. Bumps together with `kotlin`.
4. "Version reference 'ksp' is not used in build scripts" — false positive: the inspection only tracks refs consumed by `[libraries]`/`[plugins]` entries. `ksp` is consumed via `libs.versions.ksp.get()` on the root buildscript classpath, which the inspection can't see.

### Commit the 24.0.3 config set

From the repo root (`icodeforyou_android`):

```zsh
git add CatsCarsCoins/build.gradle.kts CatsCarsCoins/gradle/libs.versions.toml CatsCarsCoins/app/build.gradle.kts
git status
```

Staged list must show exactly those three files (`settings.gradle.kts` was verify-only — unchanged, won't stage).

```zsh
git commit -m "build(CatsCarsCoins): 24.0.3 Gradle config — catalog, AGP 9 built-in Kotlin wiring, Room 3, KSP 2.3.9, JVM 17, BuildConfig CAT_API_KEY"
git push
```

**Why:** the whole 24.0.3 config lands as one commit — the three files are interdependent (classpath pins ↔ versionless aliases ↔ module wiring) and only build together, so splitting them would create commits that don't sync on their own.
