# 24.5.7 — Phase 5: Koin Injection Wiring for `DbViewerViewModel`

**Project:** CatsCarsCoins (`com.icodeforyou.catscarscoins`)
**Spec section:** 24.5 — Database Viewer (Phase 5)
**Files delivered:**

```
app/src/
└── main/java/com/icodeforyou/catscarscoins/
    └── di/
        └── DbViewerModule.kt               ← REISSUE (this step)

```

**Date:** 2026-07-06

---

## Context

With `DbViewerViewModel` now verified by green JVM tests, it must be registered into the Koin dependency injection framework. This step reissues `DbViewerModule.kt` to append the view model definition using the established production DSL style layout.

## Design

1. **ViewModel DSL Registration.** We inject `DbViewerViewModel` via the standard `viewModelOf(::DbViewerViewModel)` runtime constructor mapping statement.


2. **Ambient Dependency Resolution.** The declaration automatically extracts the required `DbViewerRepository` instance and the singleton `CoinRefresher` instance (provided by the shared database infrastructure module context) straight from the graph.



## Verification gate

1. `./gradlew assembleDebug`
2. Expect: Compiles clean; dependency resolution structure is fully verified.

---

## Complete file: `DbViewerModule.kt`

```kotlin
// di/DbViewerModule.kt
// CatsCarsCoins — spec 24.5.7. Complete file.
// Change from 24.5.4: Added viewModelOf factory registration for DbViewerViewModel.
package com.icodeforyou.catscarscoins.di

import com.icodeforyou.catscarscoins.db.CatsCarsDatabase
import com.icodeforyou.catscarscoins.dbviewer.data.RoomDbViewerRepository
import com.icodeforyou.catscarscoins.dbviewer.domain.DbViewerRepository
import com.icodeforyou.catscarscoins.dbviewer.ui.DbViewerViewModel
import org.koin.androidx.viewmodel.dsl.viewModelOf
import org.koin.core.module.Module
import org.koin.dsl.module

/**
 * Dependency injection graph slice for the Phase 5 Database Viewer feature.
 * Binds the Room-backed schema reader and registers the presentation view model coordinator.
 */
val dbViewerModule: Module = module {
    single<DbViewerRepository> {
        RoomDbViewerRepository(database = get<CatsCarsDatabase>())
    }
    
    viewModelOf(::DbViewerViewModel)
}

```