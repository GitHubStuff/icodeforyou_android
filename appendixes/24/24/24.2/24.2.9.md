# 24.2.9 — Phase 2: Wire `databaseModule` into `appModules`

**Project:** CatsCarsCoins (`com.icodeforyou.catscarscoins`)
**Spec section:** 24.2 — Coins (Phase 2)
**File delivered:** `app/src/main/java/com/icodeforyou/catscarscoins/di/AppModules.kt` (complete file)
**Date:** 2026-07-05

---

## Changes

- **Added** `databaseModule` to the aggregate (second entry, after
  `preferencesModule`) and its import. Nothing else touched.

## Verification gate

```
./gradlew assembleDebug
```

Then **run on device/emulator** — this launch is the first end-to-end proof
of the Room 3 stack:

1. App launches to splash → Main. Koin registers the database definitions
   at startup (a broken definition fails here), though the `single` is
   lazy — the database file itself is created on first DAO use, which
   arrives with the repository steps.
2. No `SQLiteDriver`-related or instantiation crash in Logcat.

24.2.5's open question is already settled by your KSP run: the schema
exported flat (`app/schemas/<database FQN>/1.json`, no variant segment)
with **no `@ConstructedBy` required** on single-platform Android — recorded
here as the authoritative answer.

---

## Complete file: `AppModules.kt`

```kotlin
// di/AppModules.kt
// CatsCarsCoins — spec 24.2.9. Complete file.
// Change from 24.1.6: databaseModule wired in (Phase 2).
package com.icodeforyou.catscarscoins.di

import com.icodeforyou.catscarscoins.db.databaseModule
import com.icodeforyou.catscarscoins.preferences.di.preferencesModule
import org.koin.core.module.Module

/**
 * Aggregates every feature's Koin module (spec §3: one module per feature,
 * AppModules aggregates). Each feature phase appends its module here — the
 * only DI file MyApp ever needs to know about.
 */
val appModules: List<Module> = listOf(
    preferencesModule,
    databaseModule,
)
```
