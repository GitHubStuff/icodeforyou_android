# 24.2.16 — Phase 2: TDD Green — `String.toUsdCents()`

**Project:** CatsCarsCoins (`com.icodeforyou.catscarscoins`)
**Spec section:** 24.2 — Coins (Phase 2); spec Decision Log (money type)
**File delivered:** `app/src/main/java/com/icodeforyou/catscarscoins/coins/data/remote/UsdCents.kt` (complete file, new)
**Date:** 2026-07-05

---

## Design

- **One expression:** `BigDecimal(this).movePointRight(2)
  .setScale(0, RoundingMode.HALF_UP).longValueExact()`.
  Decimal-exact at every step; no `Double` anywhere on the path.
- `BigDecimal(String)` throws `NumberFormatException` on garbage and empty
  input — the two failure tests pass with no explicit guard code.
- `longValueExact()` over `toLong()`: a value that can't fit a Long throws
  instead of truncating silently. Unpinned by tests, chosen on principle —
  silent truncation is corruption.
- `java.math` is plain JVM — no dependency added.

## Verification gate — GREEN

```
./gradlew :app:testDebugUnitTest
```

All 31 JVM tests pass (21 prior + 10 parser).

---

## Complete file: `UsdCents.kt`

```kotlin
// coins/data/remote/UsdCents.kt
// CatsCarsCoins — spec 24.2.16. Complete file.
package com.icodeforyou.catscarscoins.coins.data.remote

import java.math.BigDecimal
import java.math.RoundingMode

/**
 * Parses a Coinbase USD amount string to exact integer cents
 * ("4343.17" → 434317). The single wire→domain conversion point for money.
 *
 * Decimal-exact by construction (spec Decision Log: never Double):
 * `toDouble() * 100` misrounds float-hostile values by a whole cent, which
 * would silently corrupt the consecutive-only dedup comparing exact cents.
 *
 * Over-precise input rounds HALF_UP (pinned by 24.2.15); malformed input
 * throws [NumberFormatException] — a bad wire amount never becomes a
 * stored sample.
 */
fun String.toUsdCents(): Long =
    BigDecimal(this)
        .movePointRight(2)
        .setScale(0, RoundingMode.HALF_UP)
        .longValueExact()
```
