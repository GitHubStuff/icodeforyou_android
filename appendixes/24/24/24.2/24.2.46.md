# 24.2.46 — Phase 2: Shared Test Double — `FakeCoinsRepository`

**Project:** CatsCarsCoins (`com.icodeforyou.catscarscoins`)
**Spec section:** 24.2 — Coins (Phase 2)
**File delivered:** `app/src/test/java/com/icodeforyou/catscarscoins/coins/domain/FakeCoinsRepository.kt` (complete file, new)
**Date:** 2026-07-05

---

## Context

The `CoinToastPresenter` tests (next step) need an emitting coins fake;
`CoinsViewModelTest` already nests one. Second user — rule of two — the
fake promotes to the canonical shared double before the duplication is
written. Same trajectory as `FakePreferencesRepository` (24.1.17).

## Design

- **Test source set, contract's package** — one canonical double per
  contract; a contract change breaks exactly one fake.
- **Honest contract behavior:** `record` prepends newest-first with a
  fresh id seeded past any initial data; `clearAll` empties.
- **`state` exposed** for direct scripting: trim-style same-head
  re-emissions, emptying the table, and post-Reset **id reuse** — the
  scenarios the presenter tests must stage precisely.
- The engine test's nested `RecordingCoinsRepository` stays put — it
  asserts *what was recorded* (write direction); this fake serves the read
  direction. Different jobs, no duplication between them.

## Follow-up queued

`CoinsViewModelTest` reissues onto this fake (next step), deleting its
nested `EmittingCoinsRepository`.

## Verification gate

None alone — unreferenced until the reissue; gate there.

---

## Complete file: `FakeCoinsRepository.kt`

```kotlin
// coins/domain/FakeCoinsRepository.kt
// CatsCarsCoins — spec 24.2.46. Complete file. Test sources.
package com.icodeforyou.catscarscoins.coins.domain

import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.flow.MutableStateFlow
import kotlinx.coroutines.flow.update

/**
 * Canonical in-memory fake of [CoinsRepository] for unit tests. Lives in
 * the test source set, same package as the contract (the
 * FakePreferencesRepository pattern). Behaves like the real thing at the
 * contract level: newest-first, [record] prepends with a fresh
 * auto-incremented id, [clearAll] empties.
 *
 * [state] is exposed so tests can script arbitrary scenarios directly —
 * trim-style re-emissions, emptying, and post-Reset id reuse — without
 * bending the honest write path.
 */
class FakeCoinsRepository(
    initial: List<Coin> = emptyList(),
) : CoinsRepository {

    val state = MutableStateFlow(initial)

    private var nextId: Long =
        (initial.maxOfOrNull { it.id } ?: Coin.UNSAVED_ID) + 1L

    override val coins: Flow<List<Coin>> = state

    override suspend fun record(amountCents: Long, recordedAtEpochMillis: Long) {
        val recorded = Coin(
            id = nextId++,
            amountCents = amountCents,
            recordedAtEpochMillis = recordedAtEpochMillis,
        )
        state.update { current -> listOf(recorded) + current }
    }

    override suspend fun clearAll() {
        state.value = emptyList()
    }
}
```
