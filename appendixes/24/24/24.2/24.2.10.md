# 24.2.10 — Phase 2: TDD Red — `RoomCoinsRepository` Instrumented Tests

**Project:** CatsCarsCoins (`com.icodeforyou.catscarscoins`)
**Spec section:** 24.2 — Coins (Phase 2); spec §24.6 test matrix
**Files delivered:** `app/src/androidTest/java/com/icodeforyou/catscarscoins/coins/data/RoomCoinsRepositoryTest.kt` (complete file, new); `app/build.gradle.kts` (complete file — androidTest classpath + coroutines core pin); `gradle/libs.versions.toml` (complete file — coroutines lockstep ref)
**Date:** 2026-07-05

---

## Context

The subject — `RoomCoinsRepository(coinDao)` in `coins/data/`, implementing
the 24.2.2 contract — does not exist yet. Per the spec's test matrix, Room
DAO behavior is tested **instrumented**: real Room 3 over
`BundledSQLiteDriver` against an in-memory database, because the trim SQL,
the ORDER BY tie-break, and Flow invalidation are precisely the behaviors a
fake would fake away.

Note the test-double asymmetry, which is deliberate: Preferences got a JVM
fake because DataStore's core is pure Kotlin; Room's value is its SQL, so
its tests run the SQL.

## Behaviors pinned (five tests)

- `record` inserts; the observed sample carries the amount, the timestamp,
  and a Room-assigned id (`> UNSAVED_ID`).
- Newest-first ordering across distinct timestamps.
- **Same-millisecond tie-break** by id descending — the 24.2.4 ordering
  clause, tested explicitly.
- **FIFO cap:** `ROW_CAP + 1` records leave exactly `ROW_CAP` rows, newest
  kept, oldest (amount 0) gone — first survivor is amount `ROW_CAP`, last
  is amount 1.
- `clearAll` empties the table.

## Correction folded in — androidTest classpath

`turbine` and `kotlinx-coroutines-test` were `testImplementation` only;
this instrumented test needs both. Two `androidTestImplementation` lines
added to `app/build.gradle.kts` (complete file delivered with this step) —
placed under `room3.testing` with the other instrumented dependencies.

## Correction folded in — coroutines core/test version skew

`connectedDebugAndroidTest` failed on device with
`NoSuchMethodError: BuildersKt.runBlockingK$default` from
`coroutines-test`'s `createTestResult`. Cause: the catalog pinned
`coroutines-test 1.11.0` but **no coroutines core artifact** — the app APK
shipped an older core resolved transitively (Compose/lifecycle), and
`runBlockingK` was added to core in 1.11.0. Two APKs, two resolutions.

Fix, lockstep by construction:
- `libs.versions.toml`: version ref renamed `coroutinesTest` →
  `coroutines`; **added** `kotlinx-coroutines-android` on the same ref;
  `kotlinx-coroutines-test` re-pointed to it.
- `app/build.gradle.kts`: **added**
  `implementation(libs.kotlinx.coroutines.android)` — the app APK now
  ships core 1.11.0 explicitly.

Both complete files delivered with this step.

## Verification gate — RED expected

```
./gradlew :app:assembleDebugAndroidTest
```

**This step passes when compilation FAILS** on unresolved
`RoomCoinsRepository`. (Green, at 24.2.11, gates on
`:app:connectedDebugAndroidTest` — device/emulator required.)

---

## Complete file: `RoomCoinsRepositoryTest.kt`

```kotlin
// coins/data/RoomCoinsRepositoryTest.kt
// CatsCarsCoins — spec 24.2.10. Complete file. androidTest sources.
package com.icodeforyou.catscarscoins.coins.data

import android.content.Context
import androidx.room3.Room
import androidx.sqlite.driver.bundled.BundledSQLiteDriver
import androidx.test.core.app.ApplicationProvider
import androidx.test.ext.junit.runners.AndroidJUnit4
import app.cash.turbine.test
import com.icodeforyou.catscarscoins.coins.domain.Coin
import com.icodeforyou.catscarscoins.db.CatsCarsDatabase
import kotlinx.coroutines.test.runTest
import org.junit.After
import org.junit.Assert.assertEquals
import org.junit.Assert.assertTrue
import org.junit.Before
import org.junit.Test
import org.junit.runner.RunWith

@RunWith(AndroidJUnit4::class)
class RoomCoinsRepositoryTest {

    private lateinit var database: CatsCarsDatabase
    private lateinit var repository: RoomCoinsRepository

    @Before
    fun setUp() {
        val context = ApplicationProvider.getApplicationContext<Context>()
        database = Room.inMemoryDatabaseBuilder<CatsCarsDatabase>(context)
            .setDriver(BundledSQLiteDriver())
            .build()
        repository = RoomCoinsRepository(database.coinDao())
    }

    @After
    fun tearDown() {
        database.close()
    }

    @Test
    fun recordedSampleIsObservedWithAssignedId() = runTest {
        repository.record(amountCents = 434317L, recordedAtEpochMillis = 1_000L)

        repository.coins.test {
            val coins = awaitItem()
            assertEquals(1, coins.size)
            assertEquals(434317L, coins.first().amountCents)
            assertEquals(1_000L, coins.first().recordedAtEpochMillis)
            assertTrue(coins.first().id > Coin.UNSAVED_ID)
        }
    }

    @Test
    fun coinsEmitNewestFirst() = runTest {
        repository.record(amountCents = 100L, recordedAtEpochMillis = 1_000L)
        repository.record(amountCents = 200L, recordedAtEpochMillis = 2_000L)
        repository.record(amountCents = 300L, recordedAtEpochMillis = 3_000L)

        repository.coins.test {
            val amounts = awaitItem().map { it.amountCents }
            assertEquals(listOf(300L, 200L, 100L), amounts)
        }
    }

    @Test
    fun sameMillisecondSamplesOrderByIdDescending() = runTest {
        repository.record(amountCents = 100L, recordedAtEpochMillis = 1_000L)
        repository.record(amountCents = 200L, recordedAtEpochMillis = 1_000L)

        repository.coins.test {
            val amounts = awaitItem().map { it.amountCents }
            assertEquals(listOf(200L, 100L), amounts)
        }
    }

    @Test
    fun recordTrimsOldestBeyondRowCap() = runTest {
        repeat(Coin.ROW_CAP + 1) { index ->
            repository.record(
                amountCents = index.toLong(),
                recordedAtEpochMillis = index.toLong(),
            )
        }

        repository.coins.test {
            val coins = awaitItem()
            assertEquals(Coin.ROW_CAP, coins.size)
            assertEquals(Coin.ROW_CAP.toLong(), coins.first().amountCents)
            assertEquals(1L, coins.last().amountCents)
        }
    }

    @Test
    fun clearAllEmptiesTheTable() = runTest {
        repository.record(amountCents = 100L, recordedAtEpochMillis = 1_000L)
        repository.record(amountCents = 200L, recordedAtEpochMillis = 2_000L)

        repository.clearAll()

        repository.coins.test {
            assertTrue(awaitItem().isEmpty())
        }
    }
}
```
