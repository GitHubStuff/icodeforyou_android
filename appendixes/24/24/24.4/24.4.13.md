# 24.4.13-correction1 — `CarApiRemoteSource` import fix

## Root cause

- `CarsRemoteSource` (24.4.7) lives in `cars/domain` — it is a domain port, alongside `CarsRepository` and `Manufacturer`.
- The 24.4.13 file assumed same-package `cars.data`, so the interface was unresolved and `fetchPage` overrode nothing.
- Fix: add the `cars.domain.CarsRemoteSource` import. No other change.

## Runbook record correction

- The 24.4.13 placement tree wrongly listed `CarsRemoteSource.kt` under `cars/data`. Corrected trees:

```
cars/
├── data/
│   ├── CarApi.kt
│   ├── CarApiRemoteSource.kt
│   ├── CarDao.kt
│   ├── CarDto.kt
│   ├── ManufacturerEntity.kt
│   └── RoomCarsRepository.kt
└── domain/
    ├── CarsRemoteSource.kt
    ├── CarsRepository.kt
    └── Manufacturer.kt
```

## File delivered (complete reissue — overwrite in place)

- `app/src/main/java/com/icodeforyou/catscarscoins/cars/data/CarApiRemoteSource.kt` — Main sources.

## Gate

Run:

```
./gradlew assembleDebug
```

- Expected: compiles clean. Counts unchanged.
