# 24.4.18 — `CarsKey` rail entry (`AppNavKeys` + `MainActivity` reissues)

## Objective

- Make the Cars destination reachable. Two reissues, **must land together** — `MainActivity` references the new `CarsKey`:
  1. `nav/AppNavKeys.kt` — `CarsKey` added.
  2. `MainActivity.kt` — rail destination + `entry<CarsKey>`.

## Changes (everything else byte-identical to disk)

- `AppNavKeys`: `CarsKey` data object inserted between `CatsKey` and `CoinsKey` — key order mirrors the rail. Header note updated.
- `MainActivity`:
  - `TOP_LEVEL_DESTINATIONS` gains Cars after Cats — rail order now **Main / Cats / Cars / Coins / Settings** (the spec order).
  - Icon: `Icons.Default.DirectionsCar` (material-icons-extended, already adopted).
  - `entryProvider` gains `entry<CarsKey> { CarsScreen() }` between the Cat entries and Coins.
  - Three imports added (`DirectionsCar`, `cars.ui.CarsScreen`, `nav.CarsKey`); alphabetical order preserved.
  - No shell behavior changes: splash orchestration, system-bar styling, chrome derivation, and the Cats detail push/pop are untouched.

## Files delivered

- `app/src/main/java/com/icodeforyou/catscarscoins/nav/AppNavKeys.kt` — Main sources. REISSUE, overwrite in place.
- `app/src/main/java/com/icodeforyou/catscarscoins/MainActivity.kt` — Main sources. REISSUE, overwrite in place.

## Placement

```
app/src/main/java/com/icodeforyou/catscarscoins/
├── MainActivity.kt
└── nav/
    └── AppNavKeys.kt
```

## Gate

Run:

```
./gradlew assembleDebug
```

Then install and eyeball on device:

```
./gradlew installDebug
```

- Expected: rail shows Main / Cats / Cars / Coins / Settings. Cars opens empty ("No manufacturers yet"); tapping Refresh fetches vPIC page 1 (~100 rows); search narrows by name and country; pull-to-refresh works; airplane-mode Refresh shows the 750 ms failure toast.
- This is the Cars vertical functionally complete — Phase 4 acceptance run is next.
