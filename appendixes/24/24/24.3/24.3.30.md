# 24.3.30 — Phase 3: TDD Green — `CatDetailViewModel`

**Project:** CatsCarsCoins (`com.icodeforyou.catscarscoins`)
**Spec section:** 24.3 — Cats (Phase 3)
**Files delivered:**

```
app/src/
└── main/java/com/icodeforyou/catscarscoins/
    └── cats/
        └── ui/
            └── CatDetailViewModel.kt       ← NEW (this step)
```

**Date:** 2026-07-06

---

## Design

1. **No second fetch.** `cats("")` (blank → all, already in the FTS cache)
   mapped to the matching id — the one-call design's payoff. A detail
   ViewModel that re-hit the network would be re-fetching data it already
   has.
2. **`firstOrNull { it.id == catId }`** — null on a missing id feeds the
   screen's not-found path; no crash, no `!!`.
3. **Observe, don't snapshot** — `stateIn` keeps the detail live: a
   background refresh touching this cat flows straight through (test 3).
4. Shared `SUBSCRIPTION_STOP_TIMEOUT_MS`, initial `null`.

## Verification gate

1. `./gradlew :app:testDebugUnitTest`
2. Expect: the three `CatDetailViewModel` tests green (73 JVM total once
   the parked engine reds are resolved; until then, filter):

```
./gradlew :app:testDebugUnitTest --tests "*CatDetailViewModelTest*"
```

---

## Complete file: `CatDetailViewModel.kt`

```kotlin
// cats/ui/CatDetailViewModel.kt
// CatsCarsCoins — spec 24.3.30. Complete file.
package com.icodeforyou.catscarscoins.cats.ui

import androidx.lifecycle.ViewModel
import androidx.lifecycle.viewModelScope
import com.icodeforyou.catscarscoins.cats.domain.Cat
import com.icodeforyou.catscarscoins.cats.domain.CatsRepository
import com.icodeforyou.catscarscoins.ui.SUBSCRIPTION_STOP_TIMEOUT_MS
import kotlinx.coroutines.flow.SharingStarted
import kotlinx.coroutines.flow.StateFlow
import kotlinx.coroutines.flow.map
import kotlinx.coroutines.flow.stateIn

/**
 * Cat detail state (Phase 3 UI). Selects the [catId] cat from the
 * repository's already-cached list — the one-call has_breeds=1 design
 * means detail needs no second fetch. Emits null when the id isn't present
 * (stale key → not-found screen). Observing (not snapshotting) keeps the
 * detail live if a background refresh updates the selected cat.
 */
class CatDetailViewModel(
    private val catId: String,
    catsRepository: CatsRepository,
) : ViewModel() {

    val cat: StateFlow<Cat?> = catsRepository.cats("")
        .map { cats -> cats.firstOrNull { it.id == catId } }
        .stateIn(
            scope = viewModelScope,
            started = SharingStarted.WhileSubscribed(SUBSCRIPTION_STOP_TIMEOUT_MS),
            initialValue = null,
        )
}
```
