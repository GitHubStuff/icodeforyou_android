# 24.3.17 — Phase 3: Wire `catsModule` into `appModules`

**Project:** CatsCarsCoins (`com.icodeforyou.catscarscoins`)
**Spec section:** 24.3 — Cats (Phase 3)
**File delivered:** `app/src/main/java/com/icodeforyou/catscarscoins/di/AppModules.kt` (complete file)
**Date:** 2026-07-05

---

## Changes

- **Added** `catsModule` (sixth entry, with the features before
  `notifierModule`) and its import. Nothing else touched.

## Verification gate

```
./gradlew assembleDebug
```

Then launch: Koin registers the cats bindings at startup — an
unresolvable `CatDao` or a broken Retrofit definition fails at launch,
not at first use. No visible change; nothing injects `CatsRepository`
until the ViewModel arrives. The entire Cats back end — Room v2, FTS4,
The Cat API with its key — is now live and dormant.

---

## Complete file: `AppModules.kt`

```kotlin
// di/AppModules.kt
// CatsCarsCoins — spec 24.3.17. Complete file.
// Change from 24.2.43: catsModule wired in (Phase 3).
package com.icodeforyou.catscarscoins.di

import com.icodeforyou.catscarscoins.cats.di.catsModule
import com.icodeforyou.catscarscoins.coins.di.coinsModule
import com.icodeforyou.catscarscoins.db.databaseModule
import com.icodeforyou.catscarscoins.net.networkModule
import com.icodeforyou.catscarscoins.notifier.notifierModule
import com.icodeforyou.catscarscoins.preferences.di.preferencesModule
import org.koin.core.module.Module

/**
 * Aggregates every feature's Koin module (spec §3: one module per feature,
 * AppModules aggregates). Each feature phase appends its module here — the
 * only DI file MyApp ever needs to know about.
 */
val appModules: List<Module> = listOf(
    preferencesModule,
    databaseModule,
    networkModule,
    coinsModule,
    catsModule,
    notifierModule,
)
```
