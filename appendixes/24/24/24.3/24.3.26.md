# 24.3.26 — Phase 3: `CatDetailKey` (Feature Nav Key)

**Project:** CatsCarsCoins (`com.icodeforyou.catscarscoins`)
**Spec section:** 24.3 — Cats (Phase 3)
**Files delivered:**

```
app/src/
└── main/java/com/icodeforyou/catscarscoins/
    └── cats/
        └── nav/
            └── CatsNavKeys.kt              ← NEW (this step)
```

**Date:** 2026-07-06

---

## Design

1. **Feature key, feature package** — the `AppNavKeys` KDoc rule pays
   out: the shell's sealed interface stays top-level-only; Cats owns its
   own keys.
2. **First parameterized key:** `data class CatDetailKey(val catId:
   String)` — the id is the payload, `@Serializable`, so a detail screen
   on top of the stack survives process death like everything else.
3. **Push, not replace:** detail will be `backStack.add(CatDetailKey(id))`
   on row tap — system back pops to the list through the already-wired
   `onBack`. No rail item; not an `AppNavKey`.
4. **No data plumbing needed:** the one-call `has_breeds=1` design cached
   everything detail renders — the ViewModel (next steps) selects by id
   from the existing observable; no new fetch, no contract change.

## Verification gate

1. `./gradlew assembleDebug`
2. Expect: clean; nothing references the key until the detail steps land.

---

## Complete file: `CatsNavKeys.kt`

```kotlin
// cats/nav/CatsNavKeys.kt
// CatsCarsCoins — spec 24.3.26. Complete file.
package com.icodeforyou.catscarscoins.cats.nav

import androidx.navigation3.runtime.NavKey
import kotlinx.serialization.Serializable

/**
 * Cats feature navigation keys (per the AppNavKeys rule: feature keys
 * live in the feature's own nav/ package; the app-shell sealed interface
 * covers only top-level destinations).
 *
 * [CatDetailKey] is the app's first parameterized key: the payload is the
 * remote cat id, @Serializable like every key so the developer-owned back
 * stack — detail included — survives process death. Detail is pushed ON
 * TOP of CatsKey (backStack.add), so system back pops naturally to the
 * list; it is not a rail destination.
 */
@Serializable
data class CatDetailKey(
    val catId: String,
) : NavKey
```
