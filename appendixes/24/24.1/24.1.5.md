# 24.1.5 — Phase 1: Koin Module for Preferences

**Project:** CatsCarsCoins (`com.icodeforyou.catscarscoins`)
**Spec section:** 24.1 — Preferences core (Phase 1)
**File delivered:** `app/src/main/java/com/icodeforyou/catscarscoins/preferences/di/PreferencesModule.kt` (complete file, new)
**Date:** 2026-07-05

---

## Design

- **`preferencesDataStore` property delegate** provides the production
  store (`app_preferences`). The delegate guarantees one instance per file
  per process; hand-constructing via `PreferenceDataStoreFactory` inside a
  DI factory is the classic source of the fatal "There are multiple
  DataStores active for the same file" `IllegalStateException`. The delegate
  is private to this file — DI is the only door.
- **Interface binding.** `single<PreferencesRepository>` binds the domain
  contract to `DataStorePreferencesRepository`; consumers inject the
  interface and never see DataStore types (DIP). The
  `single<DataStore<Preferences>>` definition exists so the repository's
  constructor is satisfied by `get()` — the same constructor the 24.1.3
  tests satisfy with a temp-file store.
- **Store name is a named constant**, appears exactly once.

## Dependency on Activity/Application wiring

`androidContext()` requires that `startKoin` in `MyApp` declares
`androidContext(this@MyApp)`. If the current `startKoin` block lacks it,
the next step's `AppModules`/`MyApp` wiring delivers that complete file —
do not hand-edit ahead of the step.

## Verification gate

```
./gradlew assembleDebug
```

Compiles clean. Behavior unchanged — `preferencesModule` is not yet in the
`appModules` aggregate; wiring is the next step.

---

## Complete file: `PreferencesModule.kt`

```kotlin
// preferences/di/PreferencesModule.kt
// CatsCarsCoins — spec 24.1.5. Complete file.
package com.icodeforyou.catscarscoins.preferences.di

import android.content.Context
import androidx.datastore.core.DataStore
import androidx.datastore.preferences.core.Preferences
import androidx.datastore.preferences.preferencesDataStore
import com.icodeforyou.catscarscoins.preferences.data.DataStorePreferencesRepository
import com.icodeforyou.catscarscoins.preferences.domain.PreferencesRepository
import org.koin.android.ext.koin.androidContext
import org.koin.dsl.module

/** On-disk store name — appears exactly once. */
private const val PREFERENCES_STORE_NAME = "app_preferences"

/**
 * Process-wide DataStore instance. The [preferencesDataStore] property
 * delegate is the canonical way to obtain a Preferences DataStore: it
 * guarantees exactly one instance per file per process. Constructing the
 * store by hand inside a DI factory is how "multiple DataStores active for
 * the same file" crashes happen — the delegate makes that impossible.
 */
private val Context.appPreferencesDataStore: DataStore<Preferences>
    by preferencesDataStore(name = PREFERENCES_STORE_NAME)

/**
 * Preferences feature wiring (spec: Koin runtime DSL, no codegen).
 * Consumers inject [PreferencesRepository] — the domain contract — and
 * never see DataStore types (Dependency Inversion).
 */
val preferencesModule = module {

    single<DataStore<Preferences>> {
        androidContext().appPreferencesDataStore
    }

    single<PreferencesRepository> {
        DataStorePreferencesRepository(dataStore = get())
    }
}
```
